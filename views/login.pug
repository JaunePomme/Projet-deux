extends layouts/frontend

block content
    .ui.middle.aligned.center.aligned.grid
        .six.wide.column
            if errorMessages.length>0 
                p.ui.message.error=errorMessages.join('<br>')
            form.ui.large.form(method="post", action="/connect")
                .ui.stacked.segment
                    +field('email', 'email', 'your.email@address.tld')
                    +field('password', 'password', 'Password')
                    button(type="submit").ui.fluid.large.teal.submit.button Login

                .ui.error.message


mixin field(type, name, placeholder)
    .field
        .ui.left.icon.input
            i.icon(class=(type === 'password' ? 'lock' : 'user'))
            input(type=type, name=name, placeholder=placeholder, required)